{
  "name": "aeims-security-tests",
  "version": "1.0.0",
  "description": "Security testing suite for AEIMS platform",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install:deps": "npx playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}